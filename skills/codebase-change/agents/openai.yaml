interface:
  display_name: "Codebase Change"
  short_description: "Plan a change from repository evidence"
  default_prompt: "Use $codebase-change to plan how this change should be implemented."
policy:
  allow_implicit_invocation: false
